# Development

Workflow: `~/.claude/skills/dev-workflow/SKILL.md` (XS / M / L tiers,
RFC for M/L, smoke test for everything, code-review subagent for M+,
incident log).

## Project-specific rules

(none yet — append as discovered)

## Tests

(populated when stack-specific extension lands a test runner)

## Pre-commit hook

См. `dev-workflow` Шаг 4 — добавляется когда появляются тесты или
линтеры в проекте. До этого живём без хука.

## Documentation discipline

См. `dev-workflow` Шаг 9 — таблица триггеров для обновления
`CLAUDE.md`, `ARCHITECTURE.md`, `CHANGELOG.md`, ADR, `incidents.md`.
